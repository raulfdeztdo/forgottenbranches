# Disable shamefully-hoist — each package must declare its own dependencies
shamefully-hoist=false

# Strict peer dependencies
strict-peer-dependencies=false

# Automatically install peers
auto-install-peers=true
