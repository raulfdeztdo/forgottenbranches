{
  "name": "forgottenbranches-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && node -e \"const fs=require('fs');const p=require('path');const s='../client/dist';const d='dist/public';if(fs.existsSync(s)){fs.cpSync(s,d,{recursive:true});console.log('  Copied client/dist → dist/public')}else{console.log('  Warning: client/dist not found. Run client build first.')}\"",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
